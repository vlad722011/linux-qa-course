# History + Aliases


!! # последняя команда
!$ # последний аргумент
!# # текущая строка
Ctrl+R # поиск назад

alias ll='ls -lah' # в ~/.bashrc
type -a ll # что выполняется
command -V ll # тип
which ll # бинарник

jobs & kill %1 # фон/убийство

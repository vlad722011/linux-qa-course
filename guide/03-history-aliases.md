# History, Aliases, Jobs

## History

echo "test" > file
cat !$ # последний аргумент
!! | head -3 # повтори + фильтр

## Aliases

~/.bashrc
alias ll='ls -lah'
alias grep='grep --color=auto'

type -a ll # проверка
